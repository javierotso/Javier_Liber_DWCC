window.onload = ()=>{
    let contador = document.getElementById("contador");
    let text = document.getElementById("text-contador");

    if(arrayCarrito.length>0){
        contador.setAttribute("display", "inline");
    }
}